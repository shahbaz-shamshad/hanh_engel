# Application Name (unchanged)
spring.application.name=WebPlatformForHanhEngel

# Railway Database Configuration (MySQL/PostgreSQL)
spring.datasource.url=${DATABASE_URL}           # Provided by Railway
spring.datasource.username=${DB_USERNAME}       # Provided by Railway
spring.datasource.password=${DB_PASSWORD}       # Provided by Railway

# Hibernate/JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect  # For MySQL
# spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect  # For PostgreSQL

# Server Port (Railway auto-assigns PORT)
server.port=${PORT:8082}  # Uses Railway's PORT or defaults to 8082

spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=shahbazinsari@gmail.com
spring.mail.password=zqqg lsvs vigx dvvp
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

spring.jpa.properties.hibernate.event.merge.entity_copy_observer=allow
spring.jpa.properties.javax.persistence.validation.mode=


spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# Load from environment variable in production
jwt.secret=${JWT_SECRET:defaultDevSecretOnlyForLocalTesting}

# Token expiration (default: 1 hour)
jwt.expiration.ms=86400000  

